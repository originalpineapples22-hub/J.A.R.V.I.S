services:
  - type: web
    name: jarvis-cloud
    runtime: python
    plan: free
    rootDir: cloud
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: JARVIS_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
